language: python
python:
  - "3.6"

install:
  - pip install python-coveralls
  - pip install coverage
  - pip install nose

script:
  - make travis
  -nosetests --with-coverage

after_success:
  - coveralls
